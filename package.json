{
  "name": "metalsmith-trial",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "node index.js",
    "deploy": "npm run build && npm run sync",
    "sync": "aws s3 sync ./public s3://www.davidharting.com/ --profile s3-deployer --acl public-read",
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "ENVIRONMENT=development npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "clean-css": "4.2.1",
    "jstransformer-handlebars": "1.1.0",
    "metalsmith": "2.3.0",
    "metalsmith-clean-css": "6.0.0",
    "metalsmith-discover-partials": "0.1.2",
    "metalsmith-layouts": "2.1.0",
    "metalsmith-markdown": "0.2.2"
  },
  "devDependencies": {}
}
